
#include "../workshop.h"


//nm -help


//ulimit can limit heap, etc

int main (void)
{
	int fd1 = open("../file1", O_RDONLY);
	int fd2= open("../file1", O_RDONLY);
	int fd3 = open("../file1", O_RDONLY);
	int fd4 = open("../file1", O_RDONLY);

	if (fd1 == -1 )

	return (0);
}